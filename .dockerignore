node_modules
**/node_modules
.git
.github
.superpowers
.tmp
# Credentials are mounted at runtime, never copied into an image.
.secrets
**/.secrets
**/google-services.json
**/*firebase-adminsdk*.json
.env
.env.local
.env.*.local
.env.external
**/.env
**/.env.*
dist
**/dist
coverage
**/coverage
.DS_Store
# Tauri/Rust native shell — not needed in any Docker image. None of the three
# images (api, worker, web) build or reference src-tauri; the web image only
# runs `vite build`. gen/android alone can reach several GB of local Gradle
# build cache, which was silently bloating the buildx context transfer.
apps/desktop/src-tauri/
